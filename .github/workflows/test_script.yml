name: Test Script

on:
  push
  workflow_dispatch:

jobs:
  go_run_test:
    runs-on: ubuntu-latest
    steps:
      - name: go test -v
        run: go test -v github.com/kod-source/test_github_actions/hello